# Raport sesji — 2026-07-29

## Cel

- Rozstrzygnąć, czy `N_MAX = 40` wystarcza dla całego zbioru.
- Powód: 11 z 18 scen (1227 z 1740 lokalizacji, 71 %) nie miało pomiaru szumu, a na `office_1`
  pojawiła się pierwsza próbka przy limicie, która nie osiągnęła progu SNR.
- Pytanie o poprawność zbioru, nie o harmonogram.

## Co zrobiono

- Nowy eksperyment `noise_floor_remaining` w `diagnose_rlr_noise.py`, zarejestrowany w `EXPERIMENTS`.
- Zmierzono 11 brakujących scen: po 2 pozycje (ułamki 0.20 i 0.75 listy lokalizacji), kąt 0°,
  M = 20 renderów, 11 konstrukcji `Simulator`.
- Zaktualizowano `GENERATOR_PARAMS.md`: §1, §3.2, §4, §4.2, §6.

## Decyzje

- Konfiguracja pomiaru **dokładnie produkcyjna** (`y` z `graph.pkl`, 1.25 m, 500 promieni, 1 wątek,
  materiały Repliki, 1 symulacja audio/render, rozgrzewka 20 renderów) — inaczej wynik nie
  odpowiadałby temu, co zobaczy generator.
- `sigma_1` estymatorem **wariancyjnym**, nie połówkowym: chodziło o dokładność referencji.
- Lista scen do pomiaru czytana z raportu diagnostycznego, nie z założeń.
- Żaden parametr specyfikacji nie został zmieniony.

## Walidacja

- **0 z 52** zmierzonych pozycji przekracza `N_MAX`; 3 z 52 przekraczają porzucony próg 24.
- Maksimum `sigma_1` = 0.10091 → `N_raw` = 31; zapas do progu 1.15×.
- Przewidywany odsetek próbek przy limicie: 0.066 % zbioru. Model skalibrowany na `office_1`
  (przewiduje 0.17 %, zaobserwowano 0.17 %).
- Werdykt: **`N_MAX = 40` wystarcza**. Projekcja czasu ≈ 29 h (21–38 h), dysk 14.0 GiB.

## Do zrobienia

- Uruchomienie produkcji, scena po scenie, począwszy od `apartment_2` (≈ 2.1 h).
- Commit zmian z tej sesji.
